name: nicbar

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build --if-present
      - run: npm start
